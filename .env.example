# Copy to .env for Docker Compose.
# These are LOCAL DEMO placeholders, not production secrets.
# Override every password before sharing the host or deploying.

DATABASE_USERNAME=tenant
DATABASE_PASSWORD=change-me-local-only
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=change-me-local-only
KEYCLOAK_AUDIENCE=task-api
SERVER_PORT=8080
RATE_LIMIT_REQUESTS=30
RATE_LIMIT_WINDOW_SECONDS=60
ANALYTICS_CACHE_TTL_SECONDS=60
