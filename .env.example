# Copy to .env (gitignored) before `docker compose up` if you want to override defaults.
# These values are local development defaults, not production secrets.

DATABASE_USERNAME=tenant
DATABASE_PASSWORD=tenant
JWT_SECRET=docker-local-jwt-secret-change-me-32chars
JWT_EXPIRATION_MS=86400000
SERVER_PORT=8080
